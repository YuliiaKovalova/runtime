# Fork-only build-failure workflow proof

> AI-generated by GitHub Copilot. This branch is an isolated test harness, not a production change.

The production source is commit `199facdb2ca775018cc5f41d7a511de4b11e7e38`
for PR `132609` in `dotnet/runtime`. The exact source and adapted file hashes
are recorded in `e2e-132609-provenance.json`.

## Identity and authentication substitutions

- The output target is `YuliiaKovalova/runtime` PR `2`, not an upstream PR.
- Existing public Azure build `1600462`, definition `129`, belongs to source
  PR `134029`. Every Azure ref/latest-build query retains that source identity.
- The mirror preserves source head `b49e9e219e667ed931ce7dcac6bafc295f1021e7`
  and base `2d8267c7a5527ec4c0c7c3bb1efe0802fae1d772`. Both merge commits must
  have exactly those parents and tree `74788c79d9a7d626503e726abd5356e2f3ba741b`
  before the Azure merge identity is translated to the fork merge identity.
- Only the fork copies replace the production PAT-pool configuration with
  ephemeral `${{ github.token }}` inference through `copilot-requests: write`.
  No personal token is transferred, uploaded, embedded, or supplied to Actions.
- The command fork guard becomes an exact fork-repository/PR allowlist.
  GitHub MCP `allowed-repos` changes to this fork; `min-integrity: none` stays.

## Trigger and fault substitutions

The automatic workflow is dispatched with an explicit `check_run` variable
adapter. Its positive runs have an empty event PR list, forcing production
ADO-derived PR resolution and head binding. This is **event replay, not proof
of genuine Azure webhook delivery**. Negative replay modes provide a mismatching
head with either an empty or populated event PR list.

The command workflow uses real `issue_comment` events. Its exact raw-command
gate, permission check, concurrency queue, and structured-analysis/request-URL
idempotency predicate are unchanged. Command comments carry a visible AI note.
Raw EOF-only parser coverage is local, because a public command comment must
also carry that disclosure.

The first actual command exposed an additional gh-aw v0.86.2 limitation:
its implicit PR checkout refuses any fork runtime. The command harness uses
the supported `checkout: false` option and explicit immutable fork checkout
instead, restoring the trusted proof commit's `.github` controls afterward.
Repository, PR number, and head are checked before that checkout. This does
not change production checkout policy, event data, or runtime library code.

Artifact faults use only test-only action inputs:

- `upload-failure`: real binlogs are staged, but the actual upload action receives
  an absent path and fails under the original `if-no-files-found: error`.
- `download-failure`: the real upload succeeds, then the actual download action
  requests a missing artifact name. An explicitly injected inert partial-file
  sentinel must be deleted by the unchanged production cleanup.
- `fail-before-analysis`: a command-only injected step fails after actual
  activation, allowing a retry of the same request URL to distinguish status
  feedback from a genuine structured analysis summary.

Neither production output flags nor action outcomes are overwritten.
Small `e2e-132609-fetch-proof` and `e2e-132609-agent-proof` artifacts contain
only allowlisted outcomes, counts, public revisions, and artifact identifiers.
They never contain a token or an environment dump.

The analyst playbook, shared prompt, normalization/extraction code, optional
handoff handling, source head checks, final latest-build check, and final
fork head/merge check are retained. The original default branch `main` is
restored after each dispatch/comment operation and on all normal error exits.
No runtime build or Azure rerun is requested.
